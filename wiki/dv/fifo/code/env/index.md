# dv fifo code env

Mirror of `src/dv/fifo/code/env/`.

This directory owns the structured testbench environment split: types, config, generator, driver, monitor, model, scoreboard, coverage, agent, env, and tracker.
