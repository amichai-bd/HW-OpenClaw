# counter physical design

The counter physical-design area exists so repository structure and QA remain
consistent across IPs.

The counter uses the same `openroad_foundation` skeleton profile as FIFO. It is
not the initial place-and-route proof of concept, but it must keep the same
configuration and wiki shape so adding real PD execution later does not require
restructuring the repository.
