# fv common

> **Historical / legacy:** Not part of the default `./build` stack. See [fv](/amichai-bd/HW-OpenClaw/wiki/fv/fv) and [software-stack](/amichai-bd/HW-OpenClaw/wiki/flows-methods-phylosophy/software-stack).

This area specifies shared formal collateral used across IPs.

Current contents:

- shared assumptions
- shared filelist content

Intent:

- common formal assumptions and helpers should live here when they are not IP-specific
- per-IP proof composition should stay under the IP-local `src/fv/<ip>/` tree

Shared formal assumptions and reusable scripts live here.
