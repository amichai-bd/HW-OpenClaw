# Physical-Design Common Scripts

Reusable physical-design scripts belong here when the OpenROAD-backed flow is wired.

Scripts in this directory should stay generic and profile-driven. IP-specific
floorplan, IO, and timing intent should be declared through YAML and generated run
collateral under `workdir/`.
