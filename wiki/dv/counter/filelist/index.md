# dv counter filelist

Mirror of `src/dv/counter/filelist/`.

DV filelists are authored relative to `$MODEL_ROOT` and translated into generated explicit filelists under `workdir/` when needed by tools.
